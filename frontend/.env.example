# Frontend env - copy to .env.local and fill in values

# API base URL (used for REST, Socket.io, and proxying /uploads/* so images work for all clients)
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Google OAuth (Web client Client ID from Google Cloud Console)
NEXT_PUBLIC_GOOGLE_CLIENT_ID="782538636653-qca4cbkrugjf405vil25asamvfsmgiqa.apps.googleusercontent.com"

# WebRTC: STUN server for peer connection (optional; defaults to Google STUN if unset)
# Example: NEXT_PUBLIC_STUN_URL="stun:stun.l.google.com:19302"
